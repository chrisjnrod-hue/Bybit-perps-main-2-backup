# Bybit endpoints (use testnet endpoints and keys for testing)
BYBIT_REST_BASE=https://api.bybit.com
BYBIT_WS_PUBLIC=wss://stream.bybit.com/realtime_public

# For testnet:
# BYBIT_REST_BASE=https://api-testnet.bybit.com
# BYBIT_WS_PUBLIC=wss://stream-testnet.bybit.com/realtime_public

# Bybit API credentials (required only if OPENTRADE=true)
BYBIT_API_KEY=
BYBIT_API_SECRET=

# Trading control
OPENTRADE=false
MAX_OPEN_TRADES=3
ROOT_TFS=60,240,D
MTF_TFS=5,15,60,240,D
ROOT_MIDSCAN_INTERVAL=0
PAGE_SIZE=50

# WebSocket / scaling
MAX_CONCURRENT_WS=20
BATCH_WS_SIZE=20

# Seeding & performance
SEED_KLINES_LIMIT=200

# Sizing & execution
DEFAULT_LEVERAGE=3
DEFAULT_TP_PERCENT=3
DEFAULT_SL_PERCENT=1.5
SPREAD_PERCENT=0.05
SLIPPAGE_PERCENT=0.1

# Breakeven modes: off | fixed | trailing_lower_highs
BREAK_EVEN_MODE=off
BREAK_EVEN_TRIGGER_PERCENT=1
BREAK_EVEN_PERCENT=0.5
TRAILING_LOOKBACK=3

# Legacy compatibility flag
BREAK_EVEN_ACTIVE=false

# Market filters (raw values from exchange)
MIN_MARKET_CAP=0                # USD (0 = disabled)
MIN_24H_USDT_VOLUME=100000      # USD (0 = disabled)
MIN_24H_VOLUME_CHANGE_PCT=-9999 # percent threshold; set e.g. 0 to require non-negative change

# CoinGecko (optional) for market cap lookup
COINGECKO_ENABLED=false

# TradingView & filters
TRADINGVIEW_ENABLED=true
TRADINGVIEW_EXCHANGE_CANDIDATES=BYBIT,BINANCE
MIN_TV_RATING=0.6
MTF_ALIGNMENT_RATING=0.6

# Exclusions
EXCLUDE_STABLES=USDT,USDC,TUSD,DAI

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_PARSE_MODE=MarkdownV2

# App & persistence
PORT=3000
LOG_LEVEL=info
DB_PATH=./data/db.sqlite

# Safety
DRY_RUN=true
TESTNET=false